meta {
  name: Update password
  type: http
  seq: 8
}

put {
  url: {{apiURL}}/auth/update-password
  body: json
  auth: none
}

body:json {
  {
    "email": "test@example.com",
    "new_password": "new password"
  }
}
